;; This require is disabled for now, because sh-mode is not
;; available via a package manager, as far as we know.
;;
;; Instead, we use the `sh-script` file in this same directory.

;(autoload 'sh-mode "sh-mode" "sh-mode" t)
