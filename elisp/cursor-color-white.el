;; Set cursor color to white - for older emacs
(set-cursor-color "white") 

;; Set cursor color to white - for newer emacs
;;(set-face-background 'cursor "white")
(put 'upcase-region 'disabled nil)
(put 'downcase-region 'disabled nil)
