;; Show the current line number and column number in the mode line,
;; which is typically the status line near the bottom of the screen.

(setq-default line-number-mode t)
(setq-default column-number-mode t)
